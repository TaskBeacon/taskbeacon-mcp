You are given a user's query for a task/repository and a list of candidate PsyFlow template repositories.

Select the **one** repository that best matches the user's intent. Consider the repository name and the README snippet for context.
Respond with **only** the repo name on a single line.
If NONE of the candidates are a reasonable match, respond with `NONE`.

